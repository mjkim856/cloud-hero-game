version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🎮 클라우드 용사 게임 빌드 시작!"
    build:
      commands:
        - echo "📦 프론트엔드 파일 준비 완료!"
        - echo "메인 파일: index.html, script.js, style.css"
    postBuild:
      commands:
        - echo "✅ 빌드 완료!"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
